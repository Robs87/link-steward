# Link Steward 产品定义文档

## 1. 产品一句话

Link Steward 是一个可 Docker 部署在 Unraid 上的自托管家庭/团队书签中枢，通过浏览器扩展收集书签、同步保存到浏览器本地书签，并提供跨设备搜索、共享、去重、整理和导出能力。

核心不是再做一个公共云收藏社区，而是让家庭或小团队拥有自己的书签服务端：数据放在自己的 Unraid 里，入口在日常使用的浏览器里，整理和导出在 Web UI 中完成。

## 2. 产品定位

### 2.1 目标定位

Link Steward = 自托管书签中枢 + 浏览器扩展输入端。

它解决的是：

- Windows、macOS、不同浏览器之间书签分散。
- 家庭或团队成员想共享资料，但浏览器书签天然偏个人。
- Chrome、Edge、Safari、Firefox 各有同步生态，跨生态迁移和统一管理困难。
- 用户看到想保存的页面时，缺少一个低摩擦、统一、可靠的保存入口。
- 用户既想集中管理书签，又想保留当前浏览器本地书签的使用习惯。
- 用户不想把所有书签托管到第三方云服务，也不想被某个平台锁定。

### 2.2 产品形态

第一版由三部分组成：

1. **Link Steward Server**
   - Docker 部署在 Unraid。
   - 作为书签主数据库和 Web API。
   - 默认数据目录位于 Unraid appdata，例如 `/mnt/user/appdata/link-steward`。

2. **Web 管理端**
   - 在浏览器中访问 Link Steward。
   - 用于搜索、标签、collection、共享、去重、导入、导出和设置。

3. **浏览器扩展**
   - Chrome / Edge 首发，采用 WebExtension Manifest V3。
   - 作为日常保存入口。
   - 支持右键保存当前页面或链接。
   - 保存到服务端后，同时写入浏览器本地书签文件夹。

### 2.3 不做什么

第一版不做：

- 公共云托管服务。
- Raindrop 式社交收藏社区。
- 移动端 App。
- 浏览器之间自动全量双向同步。
- 默认覆盖或重排用户原有浏览器书签结构。
- AI 自动批量整理、删除或移动书签。
- 复杂阅读器和稍后读系统。
- Safari 扩展和 Firefox 扩展。
- Postgres、多 workspace、外网访问安全方案的完整产品化。

## 3. 目标用户

### 3.1 第一目标用户

家庭或小团队中的跨设备重度浏览器用户：

- 家里或团队有一台 Unraid / NAS / Home Server。
- 同时使用 Windows 11 Edge、macOS Chrome 等多个设备和浏览器。
- 想把资料沉淀到一个自己控制的服务端。
- 想继续保留浏览器本地书签，而不是完全迁移到某个云收藏夹。
- 希望家庭或团队成员可以共享工具站、教程、论文、采购链接、项目资料。

### 3.2 典型用户画像

#### 用户 A：家庭技术管理员

- 家里有 Unraid，用 Docker 部署 Jellyfin、Immich、Vaultwarden 等服务。
- 想再部署一个自托管书签服务。
- 希望家人可以各自保存书签，也可以共享到家庭资料库。

#### 用户 B：跨设备工作者

- Windows 11 上用 Edge，macOS 上用 Chrome。
- 工作资料、研究链接、工具站散落在不同浏览器。
- 希望右键保存时自动进入 Link Steward，同时也留在当前浏览器本地书签中。

#### 用户 C：小团队资料管理员

- 团队成员经常分享文档、GitHub、竞品、教程、内部工具链接。
- 聊天工具里的链接容易丢。
- 希望有一个轻量的共享 collection，能按项目、主题、标签整理。

#### 用户 D：知识管理用户

- 使用 Obsidian / Notion / Markdown 管理资料。
- 希望书签可导出为 Markdown、JSON 或浏览器 HTML bookmarks。
- 关注可迁移、可备份、可审计。

## 4. 核心痛点

1. **多设备分散**：不同设备和浏览器各自维护书签，用户很难统一查找。
2. **共享困难**：家庭或团队成员之间缺少一个长期沉淀链接的地方。
3. **生态锁定**：浏览器自带同步通常服务单一生态，不适合作为跨浏览器中枢。
4. **保存入口割裂**：看到想保存的页面时，用户希望直接右键保存，而不是打开另一个网页手动复制。
5. **双重需求**：用户既想集中管理，也想继续使用浏览器本地书签栏和收藏夹。
6. **重复和脏数据**：同一个 URL 可能被多次保存，且带有 tracking 参数。
7. **整理风险高**：用户害怕自动整理或同步破坏原有书签结构。
8. **缺少上下文**：收藏时知道为什么收藏，几个月后就忘了。

## 5. 产品原则

### 5.1 自托管优先

默认数据保存在用户自己的 Unraid / Docker 服务中。第一版不提供公共云托管。

### 5.2 服务端为长期真相

Link Steward Server 是主数据库。浏览器本地书签是便利副本和用户习惯入口，不作为跨设备同步的唯一真相。

### 5.3 浏览器扩展优先

保存入口必须足够低摩擦。用户看到网页或链接时，应能通过右键菜单或扩展按钮直接保存。

### 5.4 双写但不破坏

右键保存时默认执行：

1. 保存到 Link Steward 服务端。
2. 服务端保存成功后，保存到浏览器本地 `Link Steward/Inbox` 文件夹。

浏览器本地写入失败不回滚服务端保存，而是在扩展中提示“服务端已保存，本地书签写入失败”。

### 5.5 不替用户做不可逆决定

规则或 AI 只能提出建议，不能默认批量删除、移动、覆盖或重排用户书签。

### 5.6 可迁移、可备份

用户可以随时导出：

- HTML bookmark export
- JSON
- SQLite backup
- Markdown / Obsidian 格式

## 6. 系统角色与权限

### 6.1 Owner

Owner 是服务端管理员，通常是 Unraid 管理者。

能力：

- 初始化服务端。
- 管理用户。
- 创建和管理共享 collection。
- 配置导入、导出、备份和安全策略。
- 查看系统级审计日志。

### 6.2 Member

Member 是家庭或团队成员。

能力：

- 保存个人书签。
- 使用个人库和被授权的共享 collection。
- 搜索自己可见的书签。
- 对共享 collection 添加书签、备注或标签，具体取决于权限。

### 6.3 Personal Library

每个用户都有个人书签库。默认右键保存进入个人库，也可以在扩展中选择共享 collection。

### 6.4 Shared Collections

共享 collection 用于家庭或团队共同维护资料，例如：

- 家庭采购
- AI 工具
- 项目资料
- 教程文档
- 旅游计划
- 论文和研究

## 7. MVP 范围

第一版只做“部署服务端 -> 扩展保存 -> Web 管理 -> 导入导出 -> 基础去重”的闭环。

### 7.1 Docker 服务端

支持：

- Docker 部署。
- Unraid appdata 持久化。
- SQLite 默认数据库。
- 本地账号登录。
- 单 workspace 起步。
- API token / extension pairing。
- 健康检查。
- 备份友好的目录结构。

### 7.2 Web 管理端

支持：

- 登录。
- 首页概览。
- 全局搜索。
- 个人库。
- 共享 collection。
- 标签。
- 书签详情。
- 导入 / 导出。
- 基础设置。

### 7.3 Chrome / Edge 浏览器扩展

支持：

- 配置服务端地址。
- Pairing 登录或 API token 登录。
- 右键保存当前页面。
- 右键保存链接。
- 点击扩展按钮保存当前标签页。
- 保存到 Personal Library 或 Shared Collection。
- 服务端保存成功后写入浏览器本地 `Link Steward/Inbox`。
- 识别重复 URL 并提示已收藏。
- 展示保存成功、重复、失败、本地写入失败等状态。

### 7.4 统一索引

服务端统一保存：

- Bookmark
- User
- Workspace
- Collection
- Tag
- BookmarkSource
- ExtensionDevice
- DuplicateGroup
- ChangePlan
- AuditLog

### 7.5 导入和导出

MVP 支持：

- 导入浏览器 HTML bookmarks 文件。
- 导出 HTML bookmarks 文件。
- 导出 JSON。
- 导出 Markdown / Obsidian 格式。

### 7.6 去重与清理建议

MVP 支持基础去重：

1. 完全 URL 相同。
2. URL 规范化后相同。
3. 标题 + 域名 + 路径相似。

URL 规范化规则：

- 去除 `utm_*`。
- 去除常见 tracking 参数，例如 `fbclid`、`gclid`。
- 统一协议和尾部 `/`。
- 统一大小写敏感策略。
- 可选忽略 hash。

## 8. 核心用户流程

### 8.1 服务端部署流程

1. Owner 在 Unraid 中通过 Docker 安装 Link Steward。
2. 配置 appdata 目录，例如 `/mnt/user/appdata/link-steward`。
3. 首次打开 Web UI。
4. 创建 Owner 账号。
5. 配置站点名称、内网访问地址和备份路径。
6. 创建家庭或团队成员账号。
7. 安装浏览器扩展并完成 pairing。

### 8.2 右键保存当前页面

1. 用户在 Edge / Chrome 中浏览网页。
2. 用户右键选择“保存到 Link Steward”。
3. 扩展读取当前页面标题、URL、favicon、浏览器信息。
4. 扩展调用服务端 API 保存书签。
5. 服务端规范化 URL，检查重复。
6. 服务端保存到 Personal Library 或选中的 Shared Collection。
7. 扩展在浏览器本地 `Link Steward/Inbox` 创建书签。
8. 扩展提示保存结果。

### 8.3 右键保存页面中的链接

1. 用户在某个链接上右键。
2. 用户选择“保存此链接到 Link Steward”。
3. 扩展保存链接 URL 和可得的链接文本。
4. 服务端保存并去重。
5. 扩展同步写入浏览器本地 `Link Steward/Inbox`。

### 8.4 Web 管理流程

1. 用户打开 Link Steward Web UI。
2. 搜索或浏览最近保存的书签。
3. 按用户、设备、浏览器、collection、标签过滤。
4. 编辑标题、备注、标签、collection。
5. 查看重复 URL 和清理建议。
6. 导出 HTML / JSON / Markdown。

### 8.5 团队共享流程

1. Owner 创建 Shared Collection。
2. Owner 邀请成员或给成员授权。
3. Member 在扩展中选择保存到共享 collection。
4. 其他成员可以搜索、打开、备注或继续整理该 collection。

## 9. 主要页面

### 9.1 首页 / 资产概览

展示：

- 总书签数。
- 个人书签数。
- 共享 collection 数。
- 最近保存。
- 重复 URL 数。
- tracking URL 数。
- 活跃成员。
- 已连接扩展设备。

### 9.2 全局搜索页

能力：

- 搜索标题、URL、域名、备注、标签。
- 按用户过滤。
- 按 collection 过滤。
- 按来源设备 / 浏览器过滤。
- 按状态过滤：重复、已清洗、待确认。
- 支持快速打开、复制 URL、编辑标签。

### 9.3 Collection 页面

展示：

- Personal Library。
- Shared Collections。
- Collection 内书签列表。
- 成员权限。
- 标签和备注。
- 导入 / 导出入口。

### 9.4 重复清理页

展示 DuplicateGroup：

- 标准 URL。
- 出现次数。
- 出现在哪些用户、设备、浏览器、collection。
- 推荐保留项。
- 推荐合并项。
- 风险等级。

### 9.5 Change Plan 页面

所有批量整理建议必须先进入 Change Plan。

展示：

- 将合并哪些书签。
- 将清理哪些 tracking 参数。
- 将移动到哪些 collection。
- 将影响哪些用户或共享 collection。
- before / after diff。
- 导出路径。
- 回滚或撤销策略。

### 9.6 设置页

设置：

- 服务端名称。
- 内网访问地址。
- 用户管理。
- 扩展设备管理。
- API token / pairing。
- 默认本地书签文件夹名。
- URL 清洗规则。
- 备份位置。
- 导出格式。
- 是否启用失效链接检查。
- 是否启用 AI 分类建议。

## 10. 数据模型草案

### 10.1 User

字段：

- id
- email
- display_name
- password_hash
- role：owner / member
- created_at
- last_login_at

### 10.2 Workspace

字段：

- id
- name
- owner_user_id
- created_at

第一版默认单 workspace。

### 10.3 Collection

字段：

- id
- workspace_id
- owner_user_id
- name
- type：personal / shared
- visibility：private / shared
- created_at
- updated_at

### 10.4 Bookmark

字段：

- id
- workspace_id
- created_by_user_id
- title
- url
- normalized_url
- domain
- description
- favicon_url
- collection_id
- created_at
- updated_at
- last_seen_at
- status
- metadata_json

### 10.5 BookmarkSource

字段：

- id
- bookmark_id
- source_type：extension / html_import / api / future_sync
- source_browser
- source_device
- source_profile
- source_url
- local_bookmark_folder
- created_at

### 10.6 Tag

字段：

- id
- workspace_id
- name
- color
- created_at

### 10.7 ExtensionDevice

字段：

- id
- user_id
- browser
- device_name
- extension_version
- token_hash
- last_seen_at
- created_at
- revoked_at

### 10.8 DuplicateGroup

字段：

- id
- workspace_id
- normalized_url
- bookmark_ids
- confidence
- recommended_action
- created_at

### 10.9 ChangePlan

字段：

- id
- workspace_id
- title
- status：draft / confirmed / executed / cancelled
- operations
- affected_collections
- created_by_user_id
- created_at
- executed_at

### 10.10 AuditLog

字段：

- id
- workspace_id
- actor_user_id
- action
- target_type
- target_id
- before_json
- after_json
- created_at

## 11. 技术架构建议

### 11.1 第一版技术栈

推荐：

- 服务端：TypeScript + Bun 或 Node.js。
- Web UI：React + TypeScript。
- 数据库：SQLite 默认，Postgres 后续可选。
- 搜索：SQLite FTS5。
- 扩展：WebExtension Manifest V3。
- 部署：Docker / Docker Compose。
- 目标环境：Unraid。
- 数据目录：`/mnt/user/appdata/link-steward`。

### 11.2 模块划分

- server-api
  - auth
  - users
  - bookmarks
  - collections
  - tags
  - extension-api
  - import-export
  - dedupe
  - change-plans
  - audit-logs

- web-ui
  - dashboard
  - search
  - collections
  - duplicate-cleaner
  - change-plan-viewer
  - import-export
  - settings

- browser-extension
  - manifest
  - background-service-worker
  - context-menu
  - popup
  - options
  - local-bookmark-writer

- storage
  - sqlite schema
  - migrations
  - repositories
  - fts index

## 12. 浏览器支持策略

### 12.1 第一阶段

优先支持：

- Chrome
- Edge

原因：两者可共用 Manifest V3 扩展实现。

### 12.2 第二阶段

增加：

- Firefox 扩展。
- 导入 Chrome / Edge / Firefox HTML bookmarks。
- 浏览器本地书签读取辅助。

### 12.3 第三阶段

增加：

- Safari 扩展。
- 移动端分享入口。
- 高级双向同步。

## 13. 双写策略

双写是产品体验的关键，但不能牺牲数据安全。

默认流程：

1. 扩展先调用服务端保存书签。
2. 服务端返回 bookmark id、是否重复、目标 collection。
3. 扩展再写入当前浏览器本地 `Link Steward/Inbox` 文件夹。
4. 如果本地写入成功，记录 BookmarkSource。
5. 如果本地写入失败，不回滚服务端保存，只向用户提示。

默认本地结构：

```text
Link Steward
  Inbox
```

后续可支持：

```text
Link Steward
  Inbox
  Personal
  Shared
  Projects
```

## 14. AI 能力边界

AI 可以做：

- 分类建议。
- 文件夹命名建议。
- 重复组保留建议。
- 失效链接解释。
- 收藏原因备注生成。
- Obsidian 导出摘要。

AI 不可以默认做：

- 自动删除。
- 自动移动。
- 自动覆盖浏览器本地书签。
- 自动上传数据到第三方云端。
- 自动修改共享 collection 权限。

所有 AI 建议必须可解释、可撤销。

## 15. 差异化优势

相对 OneNav、Raindrop、浏览器同步和普通书签管理器，Link Steward 的差异点：

1. 自托管在 Unraid，数据归用户自己控制。
2. 浏览器扩展是第一入口，保存动作足够低摩擦。
3. 支持家庭/团队共享 collection。
4. 同时保存到服务端和浏览器本地书签，兼顾集中管理和原有习惯。
5. 面向已有浏览器书签资产，支持导入、去重和整理。
6. 支持导出到 HTML、JSON、Markdown / Obsidian。
7. 批量整理必须经过 Change Plan，避免不可逆误操作。

## 16. OneNav 参考边界

OneNav 可以作为功能参考，但 Link Steward 不应作为 OneNav 的直接换皮版本。

### 16.1 值得借鉴

- 简单可部署的书签管理心智：一处部署、随处访问。
- 分类、二级分类、私有链接、搜索、导入导出、链接检测等成熟书签功能。
- 导航页式的快速打开体验，适合作为 Web UI 的一个视图。
- Docker 部署和 SQLite 持久化带来的低门槛。
- 浏览器扩展作为保存入口的方向。

### 16.2 不直接继承

- 不继承 PHP 单体架构；Link Steward 采用服务端 API、Web UI、浏览器扩展分离。
- 不继承单管理员个人导航站模型；Link Steward 从第一版就区分 Owner、Member、Personal Library 和 Shared Collections。
- 不把浏览器扩展当作附属功能；Link Steward 的第一入口就是扩展右键保存。
- 不依赖 OneNav 远端 API、订阅、在线升级或主题市场。
- 不复制 OneNav 品牌、界面素材、主题和商业化文案。
- 不把“可公开展示的导航站”作为主目标；Link Steward 的主目标是家庭/团队私有书签中枢。

### 16.3 兼容迁移目标

Link Steward 应支持从 OneNav 迁移数据，但迁移应作为导入器实现，而不是运行时兼容层。

第一版 OneNav 导入器可以支持：

- 从 OneNav SQLite 数据库读取 `on_categorys` 和 `on_links`。
- 将一级/二级分类映射为 collection 或 tag。
- 将 `property` 私有属性映射为个人可见性。
- 导入 `title`、`url`、`description`、`url_standby`、`font_icon`、`weight`、`click`、`check_status`。
- 生成导入报告，列出重复 URL、非法 URL 和无法映射字段。

## 17. 第一版里程碑

### M0：Docker 服务端骨架

- Docker / Docker Compose。
- SQLite 数据库。
- Web UI 壳。
- 本地账号登录。
- Owner 初始化。
- Unraid appdata 持久化。

### M1：浏览器扩展保存闭环

- Chrome / Edge 扩展。
- Pairing / API token。
- 右键保存当前页面。
- 右键保存链接。
- 保存到服务端。
- 同时写入浏览器本地 `Link Steward/Inbox`。

### M2：Web 管理端

- 首页资产概览。
- 全局搜索。
- 标签。
- Personal Library。
- Shared Collections。
- 书签详情编辑。

### M3：导入、导出、去重与 OneNav 迁移

- 导入 HTML bookmarks。
- OneNav SQLite 导入器。
- OneNav 导入预览和字段映射。
- 导出 HTML / JSON / Markdown。
- URL normalizer。
- DuplicateGroup。
- tracking 参数清理建议。
- 导入后重复识别和合并建议。

### M4：家庭/团队协作

- Member 管理。
- Shared Collection 权限。
- 审计日志。
- 共享书签备注和标签。

### M5：高级同步与 Change Plan

- 浏览器本地书签扫描。
- 服务端与浏览器本地书签比对。
- Change Plan。
- 可选回写或同步策略。

## 18. 成功指标

第一版成功不看用户量，先看闭环质量：

- 能否在 Unraid Docker 中稳定运行。
- Chrome / Edge 扩展能否顺畅完成右键保存。
- 服务端保存和浏览器本地书签双写是否可靠。
- 多用户是否能区分个人库和共享 collection。
- 搜索是否能快速定位保存过的链接。
- 导入导出是否可用且可迁移。
- 重复识别是否足够可信。
- 用户是否愿意把它作为家庭/团队资料入口。

## 19. 最大风险

1. 浏览器扩展与本地书签双写的权限和体验细节复杂。
2. 多人共享如果权限模型不清晰，会很快变乱。
3. 如果过早做浏览器双向同步，容易破坏用户本地书签信任。
4. Unraid 部署体验如果复杂，会影响第一印象。
5. 外网访问、反代、HTTPS、token 泄露等安全问题不能在 MVP 中假装已经解决。
6. 如果定位过宽，会变成低配 Raindrop 或低配协作文档库。
7. 如果直接 fork OneNav，后续会被 PHP 单体、远端订阅接口、主题机制和单用户模型拖住。

## 20. 当前结论

Link Steward 应该从“跨浏览器本机书签扫描器”调整为：

> 自托管、多人可用、浏览器扩展优先、支持本地书签双写的家庭/团队书签中枢。

第一版只要完成“Docker 服务端 -> 浏览器扩展右键保存 -> 服务端保存 + 本地书签双写 -> Web UI 搜索整理 -> HTML/JSON/Markdown 导入导出”，就已经有明确价值。浏览器全量扫描、双向同步和安全回写应放在后续高级阶段。
