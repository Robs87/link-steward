# Link Steward 开发计划

## 0. 计划目标

把 Link Steward 从本机桌面书签扫描工具重排为一条可执行的 Unraid 自托管产品路线：先完成 Docker 服务端、Web 管理端、Chrome/Edge 浏览器扩展和右键保存双写闭环，再做导入、去重、共享协作和高级同步。

MVP 核心闭环：

1. 在 Unraid 上通过 Docker 部署 Link Steward Server。
2. Owner 创建本地账号并初始化服务端。
3. 浏览器扩展完成 pairing。
4. 用户右键保存当前页面或链接。
5. 书签先保存到服务端。
6. 服务端保存成功后，扩展写入浏览器本地 `Link Steward/Inbox`。
7. 用户在 Web UI 中搜索、标签、整理、共享和导出。

## 1. 产品范围切分

### 1.1 MVP 必做

- Docker / Docker Compose 部署。
- Unraid appdata 持久化。
- SQLite 默认数据库。
- 本地账号登录。
- Owner 初始化。
- 单 workspace 起步。
- Personal Library。
- Shared Collections。
- Chrome / Edge WebExtension Manifest V3。
- Extension pairing 或 API token。
- 右键保存当前页面。
- 右键保存页面中的链接。
- 点击扩展按钮保存当前标签页。
- 服务端保存 + 浏览器本地书签双写。
- 默认本地书签文件夹 `Link Steward/Inbox`。
- 全局搜索。
- 标签。
- collection 管理。
- HTML bookmarks 导入。
- HTML / JSON / Markdown 导出。
- URL 规范化和基础去重。
- 审计日志。

### 1.2 MVP 不做

- Tauri 桌面应用。
- 公共云托管。
- 移动端 App。
- Safari 扩展。
- Firefox 扩展。
- Postgres。
- 多 workspace。
- 外网访问安全方案完整产品化。
- 浏览器之间自动全量双向同步。
- 默认覆盖或重排浏览器原有书签结构。
- AI 自动批量整理。
- 网页正文全文索引。

### 1.3 后续阶段再做

- Firefox 扩展。
- Safari 扩展。
- Postgres 可选数据库。
- 多 workspace。
- OIDC / SSO。
- 浏览器本地书签扫描。
- 服务端与浏览器本地书签比对。
- Change Plan 驱动的高级同步和回写。
- HTTPS / 反代 / 外网访问安全向导。

## 2. 推荐技术架构

### 2.1 技术栈

- 服务端：TypeScript + Bun 或 Node.js。
- Web UI：React + TypeScript。
- 数据库：SQLite 默认。
- 搜索：SQLite FTS5。
- 浏览器扩展：WebExtension Manifest V3。
- 部署：Docker / Docker Compose。
- 目标环境：Unraid。
- 数据目录：`/mnt/user/appdata/link-steward`。
- 测试：API tests、前端组件测试、扩展单元测试、Docker smoke test。

### 2.2 推荐仓库结构

```text
link-steward/
  apps/
    server/
    web/
    extension/
  packages/
    shared/
    url-normalizer/
    bookmark-import-export/
  docker/
  docs/
```

### 2.3 模块边界

```text
server-api
  auth
  users
  workspaces
  bookmarks
  collections
  tags
  extension-api
  import-export
  dedupe
  change-plans
  audit-logs

web-ui
  dashboard
  search
  collections
  bookmark-detail
  duplicate-cleaner
  change-plan-viewer
  import-export
  settings

browser-extension
  manifest
  background-service-worker
  context-menu
  popup
  options
  pairing
  local-bookmark-writer

storage
  sqlite schema
  migrations
  repositories
  fts index
```

### 2.4 核心原则落地

- 服务端是长期真相。
- 浏览器本地书签是便利副本。
- 扩展保存先写服务端，再写浏览器本地书签。
- 本地书签写入失败不回滚服务端保存。
- 所有批量整理和同步必须先生成 Change Plan。
- AI 建议只作为 suggestion，不拥有执行权限。

## 3. 数据模型与数据库计划

### 3.1 第一批表

- `users`
- `workspaces`
- `collections`
- `bookmarks`
- `bookmark_sources`
- `tags`
- `bookmark_tags`
- `extension_devices`
- `import_jobs`
- `duplicate_groups`
- `duplicate_group_items`
- `change_plans`
- `operations`
- `audit_logs`
- `settings`

### 3.2 User 字段

```text
id
email
display_name
password_hash
role
created_at
last_login_at
disabled_at
```

### 3.3 Workspace 字段

```text
id
name
owner_user_id
created_at
updated_at
```

第一版默认单 workspace。

### 3.4 Collection 字段

```text
id
workspace_id
owner_user_id
name
type
visibility
created_at
updated_at
archived_at
```

`type`：`personal` / `shared`。

### 3.5 Bookmark 字段

```text
id
workspace_id
created_by_user_id
title
url
normalized_url
domain
description
favicon_url
collection_id
created_at
updated_at
last_seen_at
status
metadata_json
```

### 3.6 BookmarkSource 字段

```text
id
bookmark_id
source_type
source_browser
source_device
source_profile
source_url
local_bookmark_folder
created_at
```

`source_type`：`extension` / `html_import` / `api` / `future_sync`。

### 3.7 ExtensionDevice 字段

```text
id
user_id
browser
device_name
extension_version
token_hash
last_seen_at
created_at
revoked_at
```

### 3.8 DuplicateGroup 字段

```text
id
workspace_id
normalized_url
confidence
recommended_action
created_at
updated_at
```

### 3.9 ChangePlan 状态机

```text
draft -> confirmed -> executed -> cancelled
```

MVP 阶段 ChangePlan 主要用于去重建议和未来同步准备，不负责默认写回浏览器原有书签结构。

### 3.10 AuditLog 字段

```text
id
workspace_id
actor_user_id
action
target_type
target_id
before_json
after_json
created_at
```

## 4. API 与扩展接口计划

### 4.1 认证 API

- `POST /api/auth/setup-owner`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/me`

### 4.2 Extension pairing API

- `POST /api/extension/pairing/start`
- `POST /api/extension/pairing/confirm`
- `GET /api/extension/me`
- `POST /api/extension/revoke`

### 4.3 书签 API

- `POST /api/bookmarks`
- `GET /api/bookmarks`
- `GET /api/bookmarks/:id`
- `PATCH /api/bookmarks/:id`
- `DELETE /api/bookmarks/:id`
- `GET /api/bookmarks/check-duplicate?url=...`

### 4.4 Collection 与标签 API

- `GET /api/collections`
- `POST /api/collections`
- `PATCH /api/collections/:id`
- `GET /api/tags`
- `POST /api/tags`

### 4.5 导入导出 API

- `POST /api/import/html-bookmarks`
- `GET /api/import/jobs/:id`
- `POST /api/export/html`
- `POST /api/export/json`
- `POST /api/export/markdown`

### 4.6 扩展保存 payload

```json
{
  "title": "Example Page",
  "url": "https://example.com/article?utm_source=x",
  "sourceBrowser": "edge",
  "sourceDevice": "Windows 11 Desktop",
  "targetCollectionId": "collection-id",
  "saveToLocalFolder": "Link Steward/Inbox"
}
```

服务端返回：

```json
{
  "bookmarkId": "bookmark-id",
  "normalizedUrl": "https://example.com/article",
  "duplicate": false,
  "targetCollectionId": "collection-id",
  "localFolder": "Link Steward/Inbox"
}
```

## 5. 双写策略

### 5.1 默认行为

右键保存时：

1. 扩展读取页面或链接信息。
2. 扩展调用 `POST /api/bookmarks`。
3. 服务端执行 URL 规范化和重复检查。
4. 服务端保存或返回重复状态。
5. 扩展使用 `chrome.bookmarks` API 写入本地 `Link Steward/Inbox`。
6. 扩展展示保存结果。

### 5.2 失败策略

- 服务端保存失败：不写浏览器本地书签，提示失败。
- 服务端保存成功、本地书签写入失败：保留服务端记录，提示“服务端已保存，本地书签写入失败”。
- URL 已存在：服务端返回 duplicate，扩展提示已收藏，可允许用户继续添加来源或标签。
- API token 失效：扩展进入重新 pairing 状态。

### 5.3 默认本地书签结构

```text
Link Steward
  Inbox
```

扩展首次写入时自动创建该文件夹。

## 6. 里程碑拆解

## M0：Docker 服务端骨架 + 数据库 + 登录

目标：Link Steward 可以作为 Unraid Docker 服务启动，并完成 Owner 初始化。

### 任务

- 初始化 monorepo。
- 建立 server、web、extension 三个 app 目录。
- 创建 Dockerfile 和 docker-compose 示例。
- 配置 Unraid appdata 数据目录。
- 建立 SQLite 连接和 migration 体系。
- 创建 users、workspaces、settings 表。
- 实现 Owner 初始化。
- 实现登录、登出、当前用户 API。
- 建立 Web UI 壳和登录页。
- 添加健康检查 endpoint。

### 验收标准

- `docker compose up` 可以启动服务。
- 数据库文件写入挂载目录。
- 首次访问 Web UI 能创建 Owner。
- Owner 可以登录和登出。
- 服务重启后账号和数据仍存在。
- 健康检查返回正常状态。

## M1：浏览器扩展保存闭环

目标：Chrome / Edge 扩展能通过右键保存页面和链接，并完成服务端 + 本地书签双写。

### 任务

- 创建 Manifest V3 扩展。
- 实现 options 页面配置服务端地址。
- 实现 pairing / API token 保存。
- 注册 context menu。
- 实现保存当前页面。
- 实现保存链接。
- 实现 popup 快速保存当前标签页。
- 实现 `POST /api/bookmarks`。
- 实现 URL normalizer 初版。
- 实现重复 URL 检查。
- 实现 `chrome.bookmarks` 写入 `Link Steward/Inbox`。
- 实现扩展保存状态提示。

### 验收标准

- Edge 和 Chrome 都能加载扩展。
- 右键当前页面可保存到服务端。
- 右键链接可保存到服务端。
- 服务端保存成功后，本地浏览器出现 `Link Steward/Inbox` 书签。
- 重复保存同一 URL 时有明确提示。
- 本地写入失败时不删除服务端记录。

## M2：Web 管理端搜索、标签、collection

目标：用户可以在 Web UI 中管理保存的书签。

### 任务

- 实现首页资产概览。
- 实现书签列表。
- 实现全局搜索。
- 建立 SQLite FTS5 索引。
- 实现标签创建和编辑。
- 实现 Personal Library。
- 实现 Shared Collection 创建。
- 实现书签详情页。
- 支持修改标题、描述、标签、collection。
- 支持按用户、浏览器、设备、collection、标签过滤。

### 验收标准

- Web UI 能看到扩展保存的书签。
- 搜索标题、URL、域名能返回结果。
- 用户可以给书签添加标签。
- 用户可以把书签移动到 Shared Collection。
- 首页统计与数据库一致。

## M3：HTML 导入/导出 + 去重

目标：支持导入旧浏览器书签，并提供基础整理能力。

### 任务

- 实现 HTML bookmarks parser。
- 实现导入任务 `import_jobs`。
- 导入时记录 `BookmarkSource`。
- 实现 HTML export。
- 实现 JSON export。
- 实现 Markdown / Obsidian export。
- 实现 DuplicateGroup 生成。
- 实现 tracking 参数清理建议。
- 实现重复清理页。

### 验收标准

- 可以导入 Chrome / Edge 导出的 HTML bookmarks。
- 导入不会覆盖已有书签。
- 导出 HTML 可被浏览器重新导入。
- JSON 导出包含来源信息。
- Markdown 导出适合 Obsidian 使用。
- 重复 URL 能生成 DuplicateGroup。

## M4：家庭/团队共享 collection 和权限

目标：家庭或团队成员能共享资料，但个人库仍保持私有。

### 任务

- 实现 Member 创建和禁用。
- 实现 Shared Collection 权限。
- 支持 collection 成员列表。
- 支持共享 collection 写入权限。
- 添加审计日志。
- 显示“谁保存了这个书签”。
- 支持按成员过滤。

### 验收标准

- Owner 可以创建 Member。
- Member 可以登录。
- Member 看不到无权限的个人 collection。
- Member 可以向授权的 Shared Collection 保存书签。
- Owner 可以查看共享 collection 的操作日志。

## M5：高级浏览器书签同步与 Change Plan

目标：在不破坏用户本地书签的前提下，探索浏览器书签扫描、比对和可审计同步。

### 任务

- 扩展读取浏览器本地 `Link Steward` 文件夹。
- 将本地书签来源同步到服务端。
- 服务端与浏览器本地书签进行差异比对。
- 生成 Change Plan。
- 展示 before / after diff。
- 支持用户确认后执行有限同步。
- 保留回滚和审计日志。

### 验收标准

- 不默认扫描或修改浏览器全部书签。
- 所有同步建议先进入 Change Plan。
- 用户确认前不改动浏览器本地书签。
- 每次同步有审计记录。

## 7. 推荐开发顺序

### 第一周：服务端骨架

- 初始化 monorepo。
- 搭建 server 和 web。
- 建立 Docker 启动流程。
- 完成 SQLite migrations。
- 完成 Owner 初始化和登录。

### 第二周：扩展最小闭环

- 搭建 Manifest V3 扩展。
- 完成 options 配置服务端。
- 完成 API token / pairing。
- 完成右键菜单。
- 完成服务端保存 API。
- 完成本地书签写入。

### 第三周：Web 管理端

- 完成 dashboard。
- 完成书签列表。
- 完成搜索和过滤。
- 完成标签和 collection。
- 完成书签详情编辑。

### 第四周：导入导出和去重

- 完成 HTML 导入。
- 完成 HTML / JSON / Markdown 导出。
- 完成 URL normalizer。
- 完成 DuplicateGroup。
- 完成重复清理页。

### 第五周：多人共享

- 完成 Member 管理。
- 完成 Shared Collection 权限。
- 完成审计日志。
- 完成共享 collection 流程 QA。

### 第六周：MVP 打磨

- 完成 Docker/Unraid 文档。
- 完成扩展安装文档。
- 完成端到端 QA。
- 修复保存失败、token 失效、重复 URL、导入异常等边界。
- 冻结高级同步到 M5。

## 8. 测试计划

### 8.1 API 测试

- Owner 初始化。
- 登录 / 登出 / 当前用户。
- 创建书签。
- 重复 URL 检查。
- 创建 collection。
- 标签增删改。
- HTML 导入任务。
- HTML / JSON / Markdown 导出。

### 8.2 Extension context menu 测试

- 右键保存当前页面。
- 右键保存链接。
- popup 保存当前标签页。
- API token 失效。
- 服务端不可用。
- 服务端保存成功但本地书签写入失败。
- 自动创建 `Link Steward/Inbox`。

### 8.3 Docker / Unraid 路径测试

- 数据库写入 `/data`。
- `/data` 映射到 `/mnt/user/appdata/link-steward`。
- 容器重启后数据保留。
- 健康检查正常。
- 日志可定位启动失败原因。

### 8.4 多用户权限测试

- Owner 创建 Member。
- Member 访问个人库。
- Member 无法访问其他用户个人库。
- Member 可以访问授权 Shared Collection。
- 未授权用户无法写入共享 collection。

### 8.5 重复 URL 保存测试

- 完全相同 URL。
- 带 `utm_*` 的 URL。
- `fbclid` / `gclid` 参数。
- 尾部 `/` 差异。
- 大小写差异。
- hash 差异。

### 8.6 HTML 导入/导出测试

- Chrome HTML bookmarks。
- Edge HTML bookmarks。
- 空文件。
- 格式错误文件。
- 重复导入同一文件。
- 导出 HTML 可被浏览器重新导入。

## 9. UI 页面开发计划

### 9.1 首页 / 资产概览

模块：

- 总书签数。
- 个人书签数。
- 共享 collection 数。
- 最近保存列表。
- 重复 URL 数。
- tracking URL 数。
- 活跃成员。
- 已连接扩展设备。

### 9.2 全局搜索页

模块：

- 搜索框。
- 用户过滤。
- 浏览器过滤。
- 设备过滤。
- collection 过滤。
- 标签过滤。
- 结果列表。
- 快速打开 / 复制 URL / 编辑标签。

### 9.3 Collection 页面

模块：

- Personal Library。
- Shared Collections。
- Collection 内书签列表。
- 成员权限。
- 标签和备注。
- 导入 / 导出入口。

### 9.4 书签详情页

模块：

- 标题。
- URL。
- normalized URL。
- 域名。
- 描述。
- 标签。
- collection。
- 来源设备 / 浏览器。
- 创建者。
- 保存时间。

### 9.5 重复清理页

模块：

- DuplicateGroup 列表。
- confidence 标识。
- 推荐保留项。
- 来源位置对比。
- 加入 ChangePlan。

### 9.6 设置页

模块：

- 服务端名称。
- 内网访问地址。
- 用户管理。
- 扩展设备管理。
- API token / pairing。
- 默认本地书签文件夹名。
- URL 清洗规则。
- 备份位置。
- 导出格式。

## 10. 关键技术决策

### 10.1 第一版不做 Tauri 桌面应用

理由：用户明确希望安装在 Unraid，并让浏览器扩展作为输入端。Tauri 会把主轴带回单机桌面，不适合作为 MVP。

### 10.2 SQLite 作为默认数据库

理由：Unraid appdata 备份简单，个人/家庭/小团队规模足够，部署门槛低。

### 10.3 Chrome / Edge 扩展首发

理由：Chrome 和 Edge 可共享 Manifest V3 实现，覆盖用户明确提到的 Windows 11 Edge 和 macOS Chrome。

### 10.4 服务端先写，本地书签后写

理由：服务端是长期真相。若先写浏览器本地书签再写服务端，服务端失败会留下不一致状态。

### 10.5 本地书签固定写入 `Link Steward/Inbox`

理由：避免污染或误改用户原有书签结构，也方便后续同步和比对。

### 10.6 高级同步必须经过 Change Plan

理由：任何会重排、删除、移动浏览器本地书签的操作都高风险，必须先展示 diff 并获得确认。

## 11. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 扩展权限复杂 | 保存体验不稳定 | MVP 只支持 Chrome/Edge，聚焦 Manifest V3 |
| 本地书签双写失败 | 用户困惑 | 服务端保存不回滚，扩展明确提示局部失败 |
| 多人权限混乱 | 共享资料泄露 | 单 workspace 起步，Personal/Shared 权限清晰隔离 |
| Unraid 部署复杂 | 初次体验差 | 提供 Docker Compose、路径约定、健康检查 |
| 外网访问安全 | token 泄露风险 | MVP 默认局域网，外网访问后续单独设计 |
| 重复判断误判 | 用户不信任 | confidence 分级，高风险只建议不执行 |
| 产品变宽 | 失焦 | 坚持书签中枢，不做社交社区和阅读器 |

## 12. MVP 完成定义

MVP 可以发布的条件：

- Docker 服务可以在 Unraid 类环境中稳定启动。
- SQLite 数据保存在挂载目录中。
- Owner 可以初始化并登录。
- Chrome / Edge 扩展可以完成 pairing。
- 扩展右键保存当前页面可写入服务端。
- 扩展右键保存链接可写入服务端。
- 服务端保存成功后，本地浏览器出现 `Link Steward/Inbox` 书签。
- Web UI 可以搜索、查看、编辑、标签和移动书签。
- Personal Library 和 Shared Collection 可用。
- HTML 导入、HTML / JSON / Markdown 导出可用。
- 重复 URL 有基础识别和提示。
- MVP 不依赖 Tauri 桌面应用。
- 浏览器全量扫描和高级回写被明确放在后续阶段。

## 13. 第一批工程任务清单

建议从这些任务开始：

1. 初始化 monorepo：`apps/server`、`apps/web`、`apps/extension`、`packages/shared`。
2. 创建 Dockerfile 和 docker-compose 示例。
3. 建立 SQLite migration 体系。
4. 实现 Owner 初始化和登录。
5. 实现 `POST /api/bookmarks`。
6. 实现 URL normalizer 单元测试。
7. 创建 Manifest V3 扩展。
8. 实现扩展 options 配置服务端地址。
9. 实现 pairing / API token 保存。
10. 实现 context menu 保存当前页面。
11. 实现 context menu 保存链接。
12. 实现 `chrome.bookmarks` 写入 `Link Steward/Inbox`。
13. 实现 Web UI 书签列表和搜索。
14. 实现 HTML bookmarks 导入。
15. 实现 HTML / JSON / Markdown 导出。
16. 做 Docker + Chrome + Edge 端到端 QA。

## 14. 待确认决策

这些不是阻塞 MVP 的决定，但后续开工前最好确认：

1. 服务端使用 Bun 还是 Node.js 运行时。
2. 是否需要从第一版就支持邀请链接，还是 Owner 手动创建成员账号即可。
3. Shared Collection 权限第一版是否只做 read/write 两级。
4. 外网访问是否计划通过反代、Tailscale、Cloudflare Tunnel 或只保留局域网。
5. 扩展是否需要提交到 Chrome Web Store / Edge Add-ons，还是先开发者模式安装。

## 15. gstack 风格审查摘要

### CEO / Scope

方向应该坚定收缩到“自托管家庭/团队书签中枢”。不要先做公共云、移动端、阅读器或复杂同步。右键保存闭环比浏览器全量扫描更高频、更应该先做。

### Product / Design

扩展保存体验是第一印象。Web UI 的重点是搜索、共享、整理和导出。Change Plan 应用于高风险批量整理，不应阻碍单次保存。

### Engineering

服务端、Web UI、扩展要从第一天共享类型和 URL normalizer。双写失败策略必须明确，否则会出现难以解释的数据不一致。

### DX / Maintainability

Docker/Unraid 部署文档、SQLite migration、API contract、扩展权限说明和 fixture 测试会决定项目后续可维护性。浏览器高级同步应被隔离为后续模块，避免污染 MVP 主线。
